1. Use python 2 for the project, some packages seems not working in python3

2. use pip install <package_name>.whl to install package

3. iclr2017_papers.csv contains 490 submission data from ICLR2017, half of them accepcted and half of them rejected.
   For a more complete dataset, we may use api.openreviews.net. 

4. preprocess.py shows some examples for preprocessing.